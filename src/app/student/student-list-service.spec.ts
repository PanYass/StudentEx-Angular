import { StudentListService } from './student-list-service';

describe('StudentListService', () => {
  it('should create an instance', () => {
    expect(new StudentListService()).toBeTruthy();
  });
});
